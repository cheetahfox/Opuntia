Index: luci/applications/luci-app-statistics/root/usr/libexec/stat-genconfig
===================================================================
--- luci.orig/applications/luci-app-statistics/root/usr/libexec/stat-genconfig
+++ luci/applications/luci-app-statistics/root/usr/libexec/stat-genconfig
@@ -136,14 +136,16 @@ function config_generic(c, singles, bool
 			if (preprocess[key])
 				c[key] = preprocess[key](c[key]);
 
-			str += _string(c[key], key, nopad);
+			if c[key]
+				str += _string(c[key], key, nopad);
 		}
 
 		for (let key in bools) {
 			if (preprocess[key])
 				c[key] = preprocess[key](c[key]);
 
-			str += _bool(c[key], key, nopad);
+			if c[key]
+				str += _bool(c[key], key, nopad);
 		}
 
 		if (lists)
